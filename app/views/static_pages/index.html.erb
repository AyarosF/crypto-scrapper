<h1>Tu veux faire du biff, du du du biff ? <br /> <span>Checke ici le cours de la <span id="crypto">cryptomonnaie</span> de ton choix !</span></h1>

<div class="formulaire">
  <%= form_tag('/') do %>
    <%= select_tag "crypto", options_for_select(@array), class: 'form-control'%>
    <%= submit_tag "Valider", class: "btn btn-outline-success" %>
  <% end %>
</div>

<% if @name %>
  <h6>Le cours actuel de la monnaie <span id="name"><%= @name %> </span> est de <span id="rate"><%= @rate %></span></h6>
<% end %>
